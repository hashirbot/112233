# Database Configuration
DATABASE_URL=postgresql://battery_user:battery_password@localhost:5432/battery_repair

# Session Security (CHANGE THIS IN PRODUCTION!)
SESSION_SECRET=your-super-secret-session-key-change-this-in-production

# Optional: PostgreSQL connection details (if not using DATABASE_URL)
PGHOST=localhost
PGPORT=5432
PGDATABASE=battery_repair
PGUSER=battery_user
PGPASSWORD=battery_password

# Flask Configuration
FLASK_ENV=production
FLASK_APP=main.py

# Docker-specific (for docker-compose.yml)
POSTGRES_DB=battery_repair
POSTGRES_USER=battery_user
POSTGRES_PASSWORD=battery_password